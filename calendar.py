# calendar program
import calendar     # importing calendar module
a = int(input('enter the year:')) # to take input from the user
b = int(input('enter month:'))
print(calendar.year(a,b)) #display the calendar
